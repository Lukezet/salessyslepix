/** @typedef {"local" | "ip" | "gps" | "default"} LocationSource */

/** @typedef {{ latitude: number, longitude: number, city: string | null, region: string | null, country: string | null, source: LocationSource }} LocationInfo */
/** @typedef {{ latitude: number, longitude: number, zoom: number }} MapViewState */

export {};
